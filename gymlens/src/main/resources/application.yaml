spring:
  application:
    name: gymlens

  datasource:
    url: jdbc:postgresql://localhost:5432/gymlensdb
    username: gymlensuser
    password: gymlenspass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
